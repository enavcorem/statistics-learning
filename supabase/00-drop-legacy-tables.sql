-- ============================================================
-- ניקוי חד-פעמי: מחיקת טבלאות מהגרסה הישנה של האפליקציה
-- ============================================================
-- להריץ פעם אחת, לפני schema.sql, ורק אחרי שאישרת שאין בטבלאות האלה
-- מידע אמיתי (אושר: "אין מידע, התוכנה הישנה לא הייתה פעילה").
--
-- הטבלאות הישנות students / quiz_answers לא תואמות למבנה החדש (חסרות,
-- למשל, עמודת "בית ספר") — צריך למחוק אותן כדי ש-schema.sql יוכל ליצור
-- אותן מחדש נכון. את module_progress לא נוגעים בה כאן — היא לא בשימוש
-- בקוד החדש, ואפשר לנקות אותה בנפרד בעתיד אם תרצי.
--
-- אזהרה: זו פעולה בלתי-הפיכה. לא להריץ שוב על פרויקט עם נתוני תלמידות
-- אמיתיים — הקובץ הזה מיועד לריצה חד-פעמית בלבד, לא חלק מתחזוקה שוטפת.

drop table if exists quiz_answers cascade;
drop table if exists students cascade;
