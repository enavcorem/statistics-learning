# לומדת הסטטיסטיקה

`COURSE_ID = 'stat'` · 36 יחידות · 73 סליידים · 210 שאלות · 7 פרקים (חלק א').

## הכלל

**`js/` `css/` `teacher/` `tools/` הם המנוע — לא נוגעים בהם כאן.**
מקור האמת: `..\..\..\lomda-platform\engine\`. שינוי מנוע נעשה שם ומסונכרן
עם `sync-engine.mjs`. כל מה שייחודי ללומדה יושב ב-`content/`.

## אחרי כל שינוי תוכן

```bash
node tools/validate-content.mjs
```

להרצה מקומית (חובה שרת — ES modules):

```bash
node "../../../lomda-platform/dev-server.mjs"
```

## דברים שחייבים לדעת לפני שנוגעים

- **`COURSE_ID` חייב להישאר `'stat'`.** ממנו נגזרים `stat_data_v3` ו-`stat_current_key_v3`
  ב-`localStorage`. שינוי = התקדמות שנראית לתלמידות כאילו נמחקה.
- **ה-`id` של היחידות כאן הוא `ch1-u1` — בלי קידומת קורס.** הלומדה הוקמה ראשונה,
  לפני שהכלל נקבע. בלומדות חדשות הקידומת חובה. לא לשנות את הקיימות.
- **`id` של פרק ≠ מספר הפרק.** הפרק החמישי ("ייצוג גרפי") הוא `id: 'ch8'`.
  `content/manifest.js` הוא מקור האמת — לא שמות התיקיות.
- **`content/ch5/` לא מקושר לאף פרק — וזה בכוונה.** תוכן שלם ששמור לחלק ב'
  (צורות התפלגות, box plot). יש שם `README.md` שמסביר. **לא למחוק.**
- **אין `classMode`** — וזה בכוונה. הנעילה ההדרגתית היא עיקר הפדגוגיה כאן.
  `?class=1` לא עושה כלום בלומדה הזו.

## מצב

- ✅ חלק א' שלם ועובר ולידציה נקייה
- ✅ עלה לגיטהאב
- ⬜ עוד לא נשלח לתלמידות אמיתיות (ההשקה מתוכננת בתוכנית העבודה של יא)
- 🔜 חלק ב' — אחוזונים, מתאם, רגרסיה (ראי ההערה בסוף `content/manifest.js`)
